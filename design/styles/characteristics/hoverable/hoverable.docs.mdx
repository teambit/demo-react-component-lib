---
description: Adds a hover effect to an element
---

import { Theme } from '@learn-harmony/design.styles.theme';
import { hoverable } from './index';

#### Adding a hover effect to an element

```jsx live
() => {
  return (
    <Theme>
      <p className={hoverable}>hoverable text</p>
    </Theme>
  );
};
```
